version: '3.9'

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:/data/prod.db
      PORT: 3000
      NODE_ENV: production
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data: